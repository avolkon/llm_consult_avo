================================================================================
  ЗАДАНИЕ НА РАЗРАБОТКУ: устранение уязвимостей и рисков по текущему аудиту ИБ
  Проект: LLM Consultation System (auth_service, bot_service, project/)
  Основание: Разработка/ИБ/ИБ_аудит.txt (открытые темы), ИБ_аудит2.txt, чек-лист
================================================================================

Цель
  Закрыть все нижеперечисленные пункты с фиксацией в коде, конфигурации и/или
  эксплуатационной документации (README, compose, примеры деплоя), согласно
  критериям приёмки. После выполнения — обновить ИБ_аудит2.txt или приложить
  короткий отчёт «закрыто» со ссылками на коммиты/PR.

Общие правила
  - Не коммитить секреты; все примеры — только в .env.example.
  - Новые зависимости — обосновать; после изменений: uv sync, pytest, ruff.
  - Регрессионные тесты для изменений в auth и bot по возможности.

================================================================================
  P0 — инфраструктура и доверие (критично до вывода в production)
================================================================================

--- Задача P0-1: Изоляция Redis и RabbitMQ ---
  Источник: аудит2 п.1; чек-лист «Сеть и инфраструктура».
  Суть: Брокер Celery и Redis не должны быть доступны анонимно из интернета.
  Работы:
    [ ] В project/docker-compose.yml (и/или отдельном compose.prod.yml): не
        публиковать порты redis/rabbitmq наружу в production-профиле; документировать.
    [ ] Заменить guest:guest на переменные окружения с сильными паролями в примерах
        для non-dev; описать в README раздел «Production».
    [ ] По возможности: Redis ACL или requirepass; RabbitMQ отдельный vhost/user.
    [ ] В README или Arch: явное требование «Redis/AMQP только во внутренней сети».
  Приёмка:
    - В сценарии «production» из документации порты 6379/5672 не слушаются на
      0.0.0.0 публичного хоста без отдельного обоснования.
    - В .env.example указаны плейсхолдеры для учётных данных брокера/Redis.

--- Задача P0-2: Модель доверия webhook MAX ---
  Источник: аудит2 п.2; ИБ_аудит.txt «открытые темы» (webhook).
  Суть: Доказать и реализовать проверку подлинности входящих запросов на webhook.
  Работы:
    [ ] Изучить документацию MAX и maxapi (FastAPIMaxWebhook): какие проверки уже есть.
    [ ] Если платформа даёт секрет/подпись: включить и задокументировать в .env.example.
    [ ] Если нет — зафиксировать компенсирующие меры (секрет в path, IP allowlist
        на ingress, отдельный middleware) в коде или в reverse proxy с ссылкой на конфиг.
    [ ] Кратко описать угрозу и решение в README (раздел про webhook).
  Приёмка:
    - Есть файл или раздел с явным ответом «как отсекается подделка webhook».
    - При необходимости — интеграционный тест или инструкция ручной проверки.

--- Задача P0-3: Секреты и доступ к окружению ---
  Источник: аудит2 п.9; чек-лист.
  Работы:
    [ ] Проверить, что OPENROUTER_API_KEY, JWT_SECRET, MAX_BOT_TOKEN не логируются.
    [ ] Для prod: описать хранение секретов (vault, K8s secrets) в документации.
  Приёмка:
    - Поиск по коду не показывает печати секретов; документация обновлена.

================================================================================
  P1 — приложение auth и bot (средний приоритет)
================================================================================

--- Задача P1-1: Лимит тела запроса надёжнее Content-Length ---
  Источник: аудит2 п.4; ИБ_аудит.txt (лимит только по Content-Length).
  Работы:
    [ ] В README/Arch: обязательный лимит на reverse proxy (пример nginx/traefik).
    [ ] По желанию в FastAPI: ограничение размера при стриминге (обрезка/отказ после N байт).
  Приёмка:
    - Документирован пример конфига прокси с client_max_body_size или аналогом.
    - При наличии доработки в коде — тест или ручной сценарий «большое тело отклонено».

--- Задача P1-2: Rate limit на POST /auth/register ---
  Источник: аудит2 п.5; ИБ_аудит.txt открытые темы.
  Работы:
    [ ] Добавить slowapi (или аналог) на /auth/register с разумным лимитом (IP).
    [ ] Учесть работу за reverse proxy (см. P2-2): доверие к X-Forwarded-For.
  Приёмка:
    - Повторные регистрации с одного IP получают 429 после порога; тест или curl-инструкция.

--- Задача P1-3: Политика паролей при регистрации ---
  Источник: аудит2 п.6.
  Работы:
    [ ] Увеличить min_length (например ≥10 или ≥12) и/или требовать классы символов.
    [ ] Опционально: простой блоклист типовых паролей (файл или небольшой список).
    [ ] Обновить тесты и сообщения об ошибках валидации.
  Приёмка:
    - Слабый пароль отклоняется; тесты pytest покрывают граничные случаи.

--- Задача P1-4: Утечка признака существования email при регистрации ---
  Источник: аудит2 п.7.
  Работы (выбрать стратегию и зафиксировать):
    [ ] Вариант A: ответ 201 для «успешно создано» и 202/200 с одинаковым телом для
        «заявка принята» без раскрытия (сложнее для UX).
    [ ] Вариант B: единый ответ без детализации + rate limit (снижает перечисление).
    [ ] Вариант C: оставить 409, но зафиксировать в политике как принятый риск.
  Приёмка:
    - В ИБ_аудит или README — явная запись о выбранной стратегии и её обосновании.

--- Задача P1-5: Минимизация логирования промптов ---
  Источник: аудит2 п.8.
  Работы:
    [ ] Убрать или заменить на хеш/длину/флаг наличие текста в INFO; детали — DEBUG
        под флагом окружения (например LOG_PROMPTS=false по умолчанию в prod).
    [ ] Не логировать ответы OpenRouter с ПДн в stdout.
  Приёмка:
    [ ] При LOG_PROMPTS=false в prod-режиме в логах нет фрагментов пользовательского текста.

--- Задача P1-6: OPENROUTER_BASE_URL — снижение риска подмены ---
  Источник: аудит2 п.9.
  Работы:
    [ ] Валидатор в config: allowlist хостов (например только openrouter.ai) или
        запрет схем file:/ нестандартных портов по политике.
    [ ] В prod при несовпадении — ошибка при старте приложения.
  Приёмка:
    [ ] Подстановка http://evil.com в OPENROUTER_BASE_URL отклоняется при старте в prod.

================================================================================
  P2 — укрепление JWT, outbox, заголовков, эксплуатация
================================================================================

--- Задача P2-1: JWT aud и allowlist alg ---
  Источник: аудит2 п.10, п.11.
  Работы:
    [ ] field_validator на jwt_alg: только «HS256» (или согласованный список).
    [ ] Опционально: добавить claim «aud» при выпуске токена и verify_aud=True при
        декодировании с фиксированным audience в конфиге (согласовать auth + bot).
  Приёмка:
    [ ] Невалидный JWT_ALG в env → падение при старте или отклонение токена.
    [ ] При внедрении aud — оба сервиса используют одно значение из конфига.

--- Задача P2-2: Rate limit за reverse proxy ---
  Источник: аудит2 п.15; ИБ_аудит.txt (slowapi in-memory / реплики).
  Работы:
    [ ] Документировать для prod: один источник истины для IP клиента (TrustedHost /
        PROXY_HEADERS) или Redis backend для slowapi.
    [ ] При горизонтальном масштабировании auth — лимит через Redis.
  Приёмка:
    - В README есть абзац «несколько реплик auth + rate limit».

--- Задача P2-3: OutboxItem — верхняя граница длины text ---
  Источник: аудит2 п.13.
  Работы:
    [ ] Pydantic Field(max_length=…) согласованно с лимитом MAX на длину сообщения.
    [ ] Отклонение/обрезка при записи в Redis; логирование без текста.
  Приёмка:
    [ ] Юнит-тест: слишком длинный text не попадает в outbox или режется по политике.

--- Задача P2-4: Security headers на bot_service (FastAPI webhook) ---
  Источник: аудит2 п.14; ИБ_аудит открытые темы.
  Работы:
    [ ] Тот же набор (nosniff, frame deny, referrer-policy) что в auth_service; HSTS
        при ENV=production.
  Приёмка:
    [ ] curl -I к /health бота показывает заголовки (в тестовом стенде).

--- Задача P2-5: Swagger /docs в production ---
  Источник: аудит2 п.16.
  Работы:
    [ ] При settings.env in {prod, production}: отключить docs и redoc или защитить
        зависимостью/basic auth.
  Приёмка:
    [ ] В prod /docs возвращает 404 или 401.

--- Задача P2-6: Prompt injection (углубление) ---
  Источник: аудит2 п.12.
  Работы (по возможности, не блокирующие полное закрытие темы у LLM):
    [ ] Документировать остаточный риск; опционально — системный промпт с политикой,
        модерация на стороне OpenRouter, лимиты токенов в API.
  Приёмка:
    - Подраздел в README «ограничения защиты от prompt injection».

--- Задача P2-7: UX /channel для JWT (снижение риска утечки в чате) ---
  Источник: аудит2 п.3.
  Работы (backlog или мини-итерация):
    [ ] Зафиксировать в README риск вставки JWT в MAX; план — короткоживущий токен
        обмена, deep link на auth, и т.д. (как в бэклоге).
  Приёмка:
    - Пользовательская документация предупреждает о канале; при появлении кода обмена —
      обновить задание.

--- Задача P2-8: Зависимости (CVE) ---
  Источник: аудит2 вступление.
  Работы:
    [ ] Внедрить в Makefile или CI: uv audit (или pip-audit); порог: 0 критичных
        без принятого исключения.
  Приёмка:
    [ ] Команда документирована; при наличии CI — job проходит.

================================================================================
  Итоговая приёмка эпика ИБ
================================================================================

  [ ] Все задачи P0–P2 отмечены выполненными или явно сняты с обоснованием в этом файле.
  [ ] Пройден Разработка/ИБ/ИБ_чеклист_уязвимостей.txt с отметками [x].
  [ ] Обновлена документация деплоя (README / Arch / отдельный DEPLOY.md при необходимости).

================================================================================
  Конец задания
================================================================================
